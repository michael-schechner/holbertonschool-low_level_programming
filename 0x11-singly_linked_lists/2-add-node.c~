#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

int _strlen(char *s)
{
	int i;

	for(i = 0; s[i] != '\0'; i++)
		;
	return (i);
}


list_t *add_node(list_t **head, const char *str)
{
	list_t *node;

	node =(malloc(sizeof(list_t));
	if (new == NULL)
		return (NULL);
	if (str != NULL)
	{
		
	}


}
